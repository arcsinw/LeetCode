#include <iostream>
#include <queue>

using namespace std;

bool isUnivalTree(TreeNode* root) {
    int value = root->val;

    queue<TreeNode*> que;

    while(is_empty(que))
    {

    }
}

int main()
{

}
